import objects.map as map

m = map.Map(100, 100,
            count=150,
            incount=1,
            per=0.15, 
            radius=2, 
            speed=0.5,
            retime=150,
            distanceRadius=0)