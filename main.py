import objects.map as map

m = map.Map(100, 100,
            count=150,
            incount=1,
            recount=0, 
            per=0.15, 
            radius=4, 
            retime=200, 
            speed=1,
            socialDistancing=False)